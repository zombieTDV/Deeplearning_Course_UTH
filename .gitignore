# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
.eggs/
.kilo

# Data — raw/processed/external data stays local
/data/raw/*
!/data/raw/.gitkeep
/data/processed/*
!/data/processed/.gitkeep
/data/external/*
!/data/external/.gitkeep
/data/*.tar.gz
/data/*.zip
/data/*.pt
/data/*.json

# Experiment outputs (track plots, results & scripts; ignore heavy weights & DBs)
/experiments/checkpoints/*.pt
!/experiments/checkpoints/.gitkeep
/experiments/runs/*
!/experiments/runs/.gitkeep
# Regenerable binary arrays (feature/probability caches) — scripts rebuild them
/experiments/results/**/*.npz
# Regenerable model weights (meta-model state dicts) — scripts rebuild them
/experiments/results/**/*.pt
/experiments/tb_logs/
/experiments/*.db
/experiments/*.sqlite

# Virtual environment
.venv/
venv/
env/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Logs
logs/
!logs/.gitkeep

# Environment
.env
.env.*

# Scratch folder
scratch/
*.tar.gz
