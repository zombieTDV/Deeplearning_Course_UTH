# Top-level runtime/dev dependencies for the project.
# Exact versions pinned in requirements.lock (pip freeze) for reproducibility.
pandas
numpy
matplotlib
tensorboard
# PyTorch with CUDA: wheels are NOT on PyPI — install via the cu130 index (see agents/bugs/BUG_01)
#   pip install torch==2.13.0+cu130 torchvision==0.28.0+cu130 --index-url https://download.pytorch.org/whl/cu130
# (plain `pip install torch` resolves to the CPU wheel and must be avoided on GPU machines)
torch==2.13.0+cu130
torchvision==0.28.0+cu130
pillow
pyyaml
optuna
scikit-learn
pytest

# Hugging Face stack (Practice 3 — roadmap T1)
transformers==5.15.0
datasets==5.0.1
evaluate==0.4.6
accelerate>=1.1.0

