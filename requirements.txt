# Top-level runtime/dev dependencies for the project.
# Exact versions pinned in requirements.lock (pip freeze) for reproducibility.
pandas
numpy
matplotlib
tensorboard
# PyTorch & Torchvision (Standard PyPI wheel compatibility)
torch
torchvision
pillow
pyyaml
optuna
scikit-learn
pytest

# Hugging Face stack (Practice 3 — roadmap T1)
transformers
datasets
evaluate
